package com.chattoorder.application.usecase;

public class ProcessIncomingMessageUseCase {

    public void process(String customerId, String message) {
        // STEP 1 — detect intent
        // STEP 2 — load conversation
        // STEP 3 — execute business action
        // STEP 4 — save updates
    }

}
