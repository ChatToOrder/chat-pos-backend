<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.10</version>
	</parent>

	<groupId>com.chattoorder</groupId>
	<artifactId>chat-pos-backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>

	<name>chat-pos-backend</name>
	<description>Chat-Based POS Backend System with WhatsApp / Instagram and AI Integration</description>
	<modules>
		<module>chat-pos-domain</module>
		<module>chat-pos-application</module>
		<module>chat-pos-infrastructure</module>
		<module>chat-pos-boot</module>
	</modules>
	<properties>
		<java.version>21</java.version>
	</properties>

	<dependencyManagement>
		<dependencies>

			<dependency>
				<groupId>com.chattoorder</groupId>
				<artifactId>chat-pos-domain</artifactId>
				<version>${project.version}</version>
			</dependency>

			<dependency>
				<groupId>com.chattoorder</groupId>
				<artifactId>chat-pos-application</artifactId>
				<version>${project.version}</version>
			</dependency>

			<dependency>
				<groupId>com.chattoorder</groupId>
				<artifactId>chat-pos-infrastructure</artifactId>
				<version>${project.version}</version>
			</dependency>

		</dependencies>
	</dependencyManagement>

</project>
